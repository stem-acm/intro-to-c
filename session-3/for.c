#include <stdio.h>

int main() {
    printf("Starting count!\n");
    for (int counter = 0; counter <= 10; counter++) {
        printf("Count: %d\n", counter);
    }

    printf("Counting finished!\n");

    return 0;
}